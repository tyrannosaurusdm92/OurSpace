# OurSpace kindness pass Cadence: **yearly** This file is stored in Shared so William and Jasper can both access it. Use the smallest safe version first. Partial completion, stopping, rest, and asking for help count. William adaptation: bed-level/seated/low-spoon versions are valid.
Jasper adaptation: ADHD transition support and caregiver-burnout protection are valid.
Schedule rule: never stack more than two items in one hour. --- # ADHD JSON + Fillable HTML Export Original JSON files are preserved in `json/`. Fillable HTML companions were added in `html/` for worksheet/planner/menu/scheduler/checklist-style JSON sources. Open `html/index.html` to browse the created forms. Each HTML file includes: - a **Print** button
- a link back to the original JSON
- local browser auto-save for typed entries
- print-friendly styling Created 10 HTML files from 22 JSON files. See `html_generation_manifest.json` for the created/skipped file list and reasons.