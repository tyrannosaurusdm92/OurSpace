# OurSpace kindness pass Cadence: **yearly** This file is stored in Shared so William and Jasper can both access it. Use the smallest safe version first. Partial completion, stopping, rest, and asking for help count. William adaptation: bed-level/seated/low-spoon versions are valid.
Jasper adaptation: ADHD transition support and caregiver-burnout protection are valid.
Schedule rule: never stack more than two items in one hour. --- # ADHD PDF JSON Exports Created: 2026-06-25T17:18:50.890535+00:00 This folder contains JSON conversions for every PDF in `adhd.zip`. ## Merge rule
PDFs were merged into one JSON group only when similarity was >= 60%, using either: - TF-IDF cosine similarity >= 0.60, or
- 5-token shingle containment of the smaller PDF >= 0.60. Containment is included because a short worksheet may be mostly embedded inside a longer guide, even when the longer guide has lots of additional material. ## Result
- PDFs found: 23
- JSON files created: 22
- Merged similar PDF groups: 1 The merged group is documented in `manifest.json` and `similarity_report.json`. ## JSON structure
Each JSON contains: - `source_documents`: source filenames, hashes, titles, page counts, word counts, and extraction counts
- `within_group_similarity`: similarity scores for files merged together
- `documents`: one or more full extracted PDF records
- each document includes `full_text` and page-by-page `text`, `text_blocks`, image metadata, links, annotations, and form field metadata where present Image binaries are not embedded in JSON; page image positions and metadata are preserved.