# Licenses and attribution notes

This module is newly written for OurSpace and is provided as project code for the user's site.

Uploaded-source observations:

- `react-music-player-and-uploader-main.zip` includes a `LICENSE` file and default Create React App content.
- `music-uploader-master.zip` includes a `LICENSE` file.
- `Made-Infinite-main.zip` package metadata lists license `MIT`.
- Other inspected projects did not all expose a clear top-level license in the inspected paths.

Because the final module does not copy source code or assets from those projects, their source files are not redistributed in this package. The docs preserve the audit trail of what was inspected and why it was not copied.

If you later copy any original project code/assets into OurSpace, keep that project's license file alongside the copied code.
